name: c-auto
with-variant:
  - native
hooks:
  build-depends:
    - "os@-dpkg"
  vcs:
    - "default"
    - "none"
    - "git"
    - "https://example.com/auto.git"
  out-of-tree: true
  config:
    - ./configure --prefix=$PREFIX
  build:
    - make -j$JOBS
  install:
    - make install
