name: test2
hooks:
  out-of-tree: false
  vcs:
    - default
    - none
    - git
    - ${TESTREPOS}/test1.bundle
  config:
    - ./configure
  build:
    - make
  install:
    - mkdir -p $PREFIX/bin $PREFIX/include
    - cp main $PREFIX/bin
    - cp incl.h $PREFIX/include
