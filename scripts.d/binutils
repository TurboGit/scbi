
function binutils-setup()
{
    PREFIX=$1
    TARGET=$2

    mkdir -p $BDIR/binutils/$TARGET

    if [ ! -d "src" ]; then
	tar xf $TARBALL/binutils.tar.gz &&
	rm -f src &&
	ln -s binutils* src
    fi
}

function binutils-config()
{
    PREFIX=$1
    TARGET=$2

    CONF_OPT="--prefix=$PREFIX --disable-multilib"

    if [ $HOST == $TARGET ]; then
	cd $BDIR/binutils/$TARGET &&
	$BDIR/binutils/src/configure --build=$HOST $CONF_OPT
    else
	cd $BDIR/binutils/$TARGET &&
	$BDIR/binutils/src/configure --target=$TARGET $CONF_OPT
    fi
}

function binutils-build()
{
    PREFIX=$1
    TARGET=$2

    cd $BDIR/binutils/$TARGET && make -j6
}

function binutils-install()
{
    PREFIX=$1
    TARGET=$2

    cd $BDIR/binutils/$TARGET && make install
}
