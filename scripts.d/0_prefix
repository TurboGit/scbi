
[ -z $ROOT ] && ROOT=$HOME/dev
[ -z $BDIR ] && BDIR=$ROOT/builds
[ -z $PREFIX ] &&
    if [ -z $(which gnatls) ]; then
        PREFIX=$BDIR/install
    else
        PREFIX=$(dirname $(dirname $(which gnatls)))
    fi
[ -z $TARBALL ] && TARBALL=$BDIR/.tar
[ -z $GTKV ] && GTKV=3.0
